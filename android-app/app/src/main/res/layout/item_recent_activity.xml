<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="3dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp"
    app:rippleColor="@color/ripple_color">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- مؤشر نوع النشاط -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/indicator_activity_type"
            android:layout_width="6dp"
            android:layout_height="0dp"
            app:cardBackgroundColor="@color/success"
            app:cardCornerRadius="3dp"
            app:cardElevation="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- الأيقونة -->
        <ImageView
            android:id="@+id/icon_activity"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/indicator_activity_type"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_sale"
            tools:tint="@color/success" />

        <!-- العنوان -->
        <TextView
            android:id="@+id/text_activity_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/on_surface"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/text_activity_time"
            app:layout_constraintStart_toEndOf="@id/icon_activity"
            app:layout_constraintTop_toTopOf="@id/icon_activity"
            tools:text="بيع جديد" />

        <!-- الوقت -->
        <TextView
            android:id="@+id/text_activity_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/on_surface_variant"
            android:textSize="11sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/text_activity_title"
            tools:text="منذ 5 دقائق" />

        <!-- العنوان الفرعي -->
        <TextView
            android:id="@+id/text_activity_subtitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/on_surface_variant"
            android:textSize="12sp"
            app:layout_constraintEnd_toStartOf="@id/text_activity_amount"
            app:layout_constraintStart_toEndOf="@id/icon_activity"
            app:layout_constraintTop_toBottomOf="@id/text_activity_title"
            tools:text="للمحل: محل الشبكات الذهبية" />

        <!-- المبلغ -->
        <TextView
            android:id="@+id/text_activity_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:background="@drawable/bg_amount_chip"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:textColor="@color/on_primary"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_activity_time"
            tools:text="1,250 ريال"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>